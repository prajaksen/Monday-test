apiVersion: v1
kind: ServiceAccount
metadata:
  name: otel-collector-sa
  namespace: llm-monitoring
  annotations:
    # IRSA placeholder: replace with your IAM role ARN for EKS IRSA
    eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/REPLACE_WITH_OTEL_COLLECTOR_ROLE
